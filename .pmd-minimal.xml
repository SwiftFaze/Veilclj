<?xml version="1.0" encoding="UTF-8"?>
<ruleset name="Veil Minimal Quality Gates"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 http://pmd.sourceforge.net/ruleset_2_0_0.xsd">

    <description>Veil quality gates: ONLY complexity (CC > 8), method length (> 40), parameters (> 4), and CPD duplication (100+ tokens)</description>

    <!-- Cyclomatic Complexity: fail if method CC > 8; no class-level aggregate check -->
    <rule ref="category/java/design.xml/CyclomaticComplexity">
        <properties>
            <property name="methodReportLevel" value="8"/>
            <property name="classReportLevel" value="9999"/>
        </properties>
    </rule>

    <!-- Excessive Parameter List: fail if > 4 parameters (5+ fails, 4 or fewer
         passes). Confirmed empirically 2026-09-06 against PMD 7.17.0 that this
         rule's `minimum` property was a strictly-greater-than threshold, not a
         minimum-to-trigger — minimum=4 was what made 5 the first value that
         failed. #215's PMD 7.27.0 bump changed that semantic back to
         minimum-to-trigger (>=): with minimum=4, 4-parameter methods started
         failing too (verified: Player.render(Graphics2D, int, int, Camera), 35
         repo-wide). minimum=5 is what restores the original 5-fails/4-passes
         behavior under 7.27.0 - re-verify this comment if the PMD version
         changes again. -->
    <rule ref="category/java/design.xml/ExcessiveParameterList">
        <properties>
            <property name="minimum" value="5"/>
        </properties>
    </rule>

    <!-- Method Length: fail if > 40 lines (NCSS = non-comment source statements) -->
    <rule ref="category/java/design.xml/NcssCount">
        <properties>
            <property name="methodReportLevel" value="41"/>
        </properties>
    </rule>

</ruleset>
